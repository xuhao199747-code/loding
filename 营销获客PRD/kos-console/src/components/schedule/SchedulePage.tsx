import { useState } from "react";
import { CalendarClock, CheckCircle2, Phone, RefreshCw } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Tabs, TabsList, TabsTrigger } from "@/components/ui/tabs";

export type Task = { name: string; detail: string; action: string; tag: string; status?: string };
export const initialTasks: Task[] = [{ name: "林女士 · 川西稻城", detail: "已看报价，等待同行人确认", action: "立即联系", tag: "报价待确认" }, { name: "赵先生 · 三亚亲子", detail: "今晚 20:00 发送酒店升级方案", action: "打开对话", tag: "高意向" }, { name: "陈女士 · 新疆包车", detail: "周五出发，待确认接送机", action: "出行服务", tag: "48 小时内" }, { name: "刘先生 · 关西自由行", detail: "去年同期游客，推荐赏枫线路", action: "复购召回", tag: "复购" }];
export function SchedulePage({ sharedTasks, onTasksChange }: { sharedTasks?: Task[]; onTasksChange?: (tasks: Task[]) => void }) {
  const [localTasks, setLocalTasks] = useState(initialTasks);
  const tasks = sharedTasks ?? localTasks;
  const updateTasks = (updater: (items: Task[]) => Task[]) => { const next = updater(tasks); if (onTasksChange) onTasksChange(next); else setLocalTasks(next); };
  const [refreshed, setRefreshed] = useState(false);
  const [filter, setFilter] = useState("pending");
  const act = (task: Task) => updateTasks((items) => items.map((item) => item.name === task.name ? { ...item, status: "已联系" } : item));
  const visibleTasks = tasks.filter((task) => filter === "all" || (filter === "pending" && !task.status) || (filter === "done" && task.status === "已联系"));
  const completedCount = tasks.filter((task) => task.status === "已联系").length;
  return <div className="space-y-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-sm font-medium text-primary">TRAVEL OPS · SERVICE SCHEDULER</p><h2 className="mt-1 text-2xl font-bold tracking-tight">跟进调度中心</h2><p className="mt-2 text-sm text-muted-foreground">让报价、支付、出发提醒和复购跟进按时发生。</p></div><Button variant="outline" onClick={() => setRefreshed(true)}><RefreshCw className="h-4 w-4" />刷新任务</Button></div>{refreshed && <div role="status" className="rounded-lg bg-emerald-50 px-4 py-3 text-sm text-emerald-700">任务已刷新，优先级已重新排序。</div>}{completedCount > 0 && <div role="status" className="rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700">任务已完成：<span>已联系</span> {completedCount} 项</div>}<div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[["今日待跟进", String(tasks.filter((task) => !task.status).length), "高意向 8 条"], ["已联系", String(completedCount), "今日已完成"], ["48 小时内出发", "17", "需完成出行提醒"], ["复购召回", "31", "近 90 天出行游客"]].map(([label, value, delta]) => <Card key={label}><CardHeader className="pb-2"><CardTitle className="text-xs text-muted-foreground">{label}</CardTitle></CardHeader><CardContent><p className="text-2xl font-bold">{value}</p><p className="mt-2 text-xs text-muted-foreground">{delta}</p></CardContent></Card>)}</div><div className="grid gap-4 lg:grid-cols-[1.2fr_.8fr]"><Card><CardHeader><div className="flex flex-wrap items-center justify-between gap-3"><CardTitle className="flex items-center gap-2"><CalendarClock className="h-5 w-5 text-primary" />今日跟进队列</CardTitle><Tabs value={filter} onValueChange={setFilter}><TabsList aria-label="任务状态"><TabsTrigger value="pending">待处理</TabsTrigger><TabsTrigger value="done">已完成</TabsTrigger><TabsTrigger value="all">全部</TabsTrigger></TabsList></Tabs></div></CardHeader><CardContent className="space-y-2">{visibleTasks.length ? visibleTasks.map((task) => <div key={task.name} className="flex flex-wrap items-center gap-3 rounded-lg border p-3"><div className="min-w-0 flex-1"><p className="font-medium">{task.name}</p><p className="mt-1 text-xs text-muted-foreground">{task.detail}</p></div><Badge variant="secondary">{task.status ?? task.tag}</Badge><Button size="sm" variant="outline" onClick={() => act(task)} disabled={task.status === "已联系"}>{task.status ?? task.action}</Button></div>) : <p className="py-8 text-center text-sm text-muted-foreground">暂无{filter === "done" ? "已完成" : "待处理"}任务</p>}</CardContent></Card><Card><CardHeader><CardTitle>服务节奏</CardTitle></CardHeader><CardContent className="space-y-5"><div><div className="flex justify-between text-sm"><span>报价响应 SLA</span><strong>平均 6 分钟</strong></div><div className="mt-2 h-2 rounded-full bg-muted"><div className="h-full w-[82%] rounded-full bg-primary" /></div></div><div><div className="flex justify-between text-sm"><span>支付转化</span><strong>今日 34.6%</strong></div><div className="mt-2 h-2 rounded-full bg-muted"><div className="h-full w-[68%] rounded-full bg-emerald-500" /></div></div><div><div className="flex justify-between text-sm"><span>出发前提醒</span><strong>已完成 82%</strong></div><div className="mt-2 h-2 rounded-full bg-muted"><div className="h-full w-[82%] rounded-full bg-amber-500" /></div></div><p className="flex items-center gap-2 text-xs text-emerald-600"><CheckCircle2 className="h-4 w-4" /><Phone className="h-4 w-4" />优先级已自动排序</p></CardContent></Card></div></div>;
}
