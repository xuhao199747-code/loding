# RAG 技术方案产品经理版简化设计

## 目标

将飞书文档“RAG 技术方案”改为产品经理可理解、可复述、且与项目原始材料一致的版本。保留一张整体流程图，删除重复流程和不必要的工程实现细节。

## 内容结构

章节保留四部分：

1. RAG 方案：说明基础 Hybrid Recall 与后续 LightRAG 图谱增强的分工，不写成互相替代。
2. RAG 技术选型：只保留产品经理需要掌握的四项——Hybrid Recall、LightRAG、Embedding/实体关系抽取、Re-Ranker。
3. RAG 整体流程：仅保留一张业务级流程图。
4. Re-Ranker 选型与状态：保留 Qwen3-Reranker-4B 原文选型，同时明确当前版本暂未上线。

## 流程图

流程图表达为：

数据来源（AI Answer、用户文档、行业知识） → 清洗与切分 → 基础索引与 LightRAG 图谱加工 → 知识检索接口 → Hybrid Recall（BM25 + Vector，按需图谱查询） → Optional Re-Ranker（可选重排序，暂未上线） → Agent/LLM 生成答案。

## 删除与修正

- 删除“材料没有说明两部分如何统一路由”的错误判断。
- 删除独立的 Re-Ranker 流程图，避免与整体流程重复。
- 删除 Token 数、向量维度、Workspace 派生、内部节点名称和存储表结构等工程细节。
- 不把 Qwen3-Reranker-4B 写成已上线，只标记为原文选型。
- 不把 LightRAG 写成已经替代基础召回，只描述为后续增加的图谱增强能力。

## 验收标准

- 正文能够在一分钟内讲清方案、分工和当前状态。
- 只保留一张流程图，英文术语后带中文。
- “已实现、已集成、规划中、暂未上线”不混写。
- Qwen3-Reranker-4B、text-embedding-v4、Qwen3 实体关系抽取等原文信息不被误删或误改。
